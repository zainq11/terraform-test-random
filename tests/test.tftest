step "eval" {
    variables = {
        number = 3
    }

    postcondition {
        condition = length(outputs.names) == 3
        error_message = "wrong length of outputs ${length(outputs.names)}
    }
}